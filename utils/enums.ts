export enum MethodAll {
    USERS,
    PROJECTS
}

export enum Method {
    USER,
    PROJECT
}

export enum Types {
    STRING = "string",
    NUMBER = "number",
    BOOLEAN = "boolean",
    NUMBER_ARRAY = "object",
    OBJECT = "object"
}